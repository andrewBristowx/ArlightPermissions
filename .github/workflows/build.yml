name: Compilar ArlightPermissions

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - 'agent/**'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven
      - name: Validar versión
        shell: bash
        run: |
          set -euo pipefail
          grep -q '<version>1.0.4</version>' pom.xml
          grep -q "version: '1.0.4'" src/main/resources/plugin.yml
      - name: Compilar y probar
        run: mvn -B -U clean package
      - name: Reunir JAR
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          find target -maxdepth 1 -type f -name 'ArlightPermissions-1.0.4.jar' -exec cp {} dist/ \;
          test -f dist/ArlightPermissions-1.0.4.jar
      - uses: actions/upload-artifact@v4
        with:
          name: ArlightPermissions-1.0.4-JAR
          path: dist/ArlightPermissions-1.0.4.jar
          if-no-files-found: error
